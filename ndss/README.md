# Crafter

#### 介绍

本项目实现了一种保护人脸图像中可识别身份信息、抵御逆向重建攻击的防御机制，在不修改后端主干模型结构的前提下，在边缘侧对原始人脸图像进行轻量变换，将受保护表征发送至云端完成推理或训练，作为即插即用的预处理插件部署在通用边缘–云协同框架中。项目在CelebA、LFW等数据集与ResNet18、VGG16等模型上评估防御前后的效用–隐私权衡，证明在削弱逆向自适应重建攻击的同时，仍能保持较高任务精度。
